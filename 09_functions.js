function add_two_numbers(number1,number2){   //no need to define argument type
console.log(number1+number2);
}

add_two_numbers(3,5)  


//we can also return values from function-


function prod(number1,number2){
return number1*number2
}


const result= prod(5,6)

console.log(`result is ${result}`);